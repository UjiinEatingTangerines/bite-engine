# v0와 BiteEngine 통합 가이드

## 🎨 v0를 사용한 디자인 워크플로우

### 방법 1: v0에서 새 컴포넌트 생성 후 가져오기 (추천)

#### 1.1 v0 접속 및 프롬프트 작성

1. https://v0.dev 접속
2. 새 컴포넌트 생성 프롬프트:

```
BiteEngine 프로젝트용 컴포넌트를 만들어줘.

디자인 시스템:
- Dark theme (midnight blue: oklch(0.13 0.02 260))
- Primary color: neon indigo (oklch(0.65 0.25 280))
- Accent color: neon emerald (oklch(0.7 0.2 160))
- shadcn/ui (New York style) 사용
- Tailwind CSS v4
- 한국어 UI

컴포넌트 요구사항:
[여기에 원하는 컴포넌트 설명 입력]

예시:
"투표 현황을 보여주는 대시보드 카드를 만들어줘.
실시간 투표 수, 참여율, 그리고 막대 그래프를 포함해야 해."
```

#### 1.2 생성된 코드 복사

v0에서 생성된 코드를 복사:
- Dependencies 확인 (필요시 설치)
- Component 코드 복사

#### 1.3 프로젝트에 통합

```bash
# 1. 새 의존성 설치 (v0가 제안한 것)
pnpm add [package-name]

# 2. 컴포넌트 파일 생성
# components/[component-name].tsx

# 3. 기존 페이지에 통합
# app/page.tsx 또는 다른 페이지에서 import
```

#### 예시 워크플로우

**v0 프롬프트**:
```
회식 투표 히스토리를 보여주는 타임라인 컴포넌트를 만들어줘.
- 각 회식마다 날짜, 장소, 참석자 수 표시
- 다크 테마
- 스크롤 가능한 세로 타임라인
- 각 항목에 hover 효과
```

**생성된 코드 가져오기**:
```typescript
// components/dinner-history-timeline.tsx
"use client"

import { motion } from "framer-motion"
import { Calendar, Users, MapPin } from "lucide-react"
import { Card } from "@/components/ui/card"

// v0에서 생성된 코드 붙여넣기
export function DinnerHistoryTimeline({ dinners }) {
  // ...
}
```

**페이지에 통합**:
```typescript
// app/history/page.tsx
import { DinnerHistoryTimeline } from "@/components/dinner-history-timeline"

export default function HistoryPage() {
  return (
    <div>
      <DinnerHistoryTimeline dinners={dinnerData} />
    </div>
  )
}
```

---

### 방법 2: v0 프로젝트 Export 후 수정

#### 2.1 v0에서 전체 프로젝트 생성

1. v0.dev에서 "Create new project" 클릭
2. 프롬프트 입력:

```
BiteEngine이라는 회식 투표 웹앱을 만들어줘.

기능:
1. 레스토랑 카드 그리드
2. 투표 버튼 (각 레스토랑마다)
3. 실시간 투표 현황 사이드바
4. 식이 필터 (비건, 글루텐 프리 등)
5. AI 추천 섹션

디자인:
- Dark theme (midnight blue)
- Neon indigo/emerald accents
- 한국어 UI
- shadcn/ui components
- Framer Motion animations

데이터 구조:
- 레스토랑: name, cuisine, rating, distance, votes
- 활동 피드: user, action, restaurant, timestamp
```

#### 2.2 코드 Export

- v0에서 "Export" 버튼 클릭
- "Download ZIP" 선택
- 압축 해제

#### 2.3 기존 프로젝트와 병합

```bash
# v0 프로젝트 압축 해제
unzip v0-biteengine.zip -d /tmp/v0-project

# 필요한 컴포넌트만 선택적으로 복사
cp /tmp/v0-project/components/new-component.tsx components/
cp /tmp/v0-project/app/new-page/page.tsx app/new-page/

# 또는 전체 교체
# (주의: 기존 커스터마이징 백업 필수!)
```

---

### 방법 3: v0 디자인 시스템만 활용

#### 3.1 v0에서 디자인 토큰 생성

v0 프롬프트:
```
BiteEngine용 Tailwind CSS 디자인 시스템을 만들어줘.

Colors:
- Background: midnight blue (oklch(0.13 0.02 260))
- Primary: neon indigo (oklch(0.65 0.25 280))
- Accent: neon emerald (oklch(0.7 0.2 160))

Components:
- Button variants (primary, secondary, ghost)
- Card styles (default, highlighted, winner)
- Badge styles (AI 추천, 가성비, 사무실 근처)
- Input styles (dark theme)

Animations:
- Hover effects
- Loading states
- Transition durations
```

#### 3.2 생성된 스타일 적용

```css
/* app/globals.css에 추가 */
/* v0에서 생성된 커스텀 클래스들 */
```

---

## 🔄 실시간 디자인 수정 워크플로우

### 일일 작업 흐름

1. **기능 요구사항 정의**
   ```
   오늘 할 일: 레스토랑 상세 모달 추가
   ```

2. **v0에서 프로토타입**
   ```
   프롬프트: "레스토랑 상세 정보를 보여주는 모달을 만들어줘.
   - 큰 이미지
   - 메뉴, 가격, 리뷰
   - 예약하기 버튼
   - 다크 테마"
   ```

3. **코드 복사 & 수정**
   ```bash
   # components/restaurant-modal.tsx 생성
   # v0 코드 붙여넣기
   # 실제 데이터 연결
   ```

4. **테스트 & 커밋**
   ```bash
   pnpm dev  # localhost 확인
   git add components/restaurant-modal.tsx
   git commit -m "feat: Add restaurant detail modal"
   git push
   ```

---

## 🎯 v0 프롬프트 베스트 프랙티스

### 효과적인 프롬프트 작성법

#### ✅ Good Example
```
회식 투표 결과를 보여주는 우승자 발표 카드를 만들어줘.

요구사항:
- 중앙에 큰 트로피 아이콘
- 레스토랑 이름 (큰 글씨, 그라디언트)
- 총 투표 수와 참여율 표시
- "예약 완료" 배지
- confetti 애니메이션 (framer-motion)
- 다크 테마 (midnight blue background)
- 네온 인디고/에메랄드 accents
- 한국어 텍스트

데이터 구조:
{
  restaurant: string,
  votes: number,
  totalVoters: number,
  bookingTime: string
}
```

#### ❌ Bad Example
```
우승자 카드 만들어줘
```

### 프롬프트 템플릿

```
[컴포넌트명]을 만들어줘.

UI 요구사항:
- [레이아웃 설명]
- [표시할 정보]
- [인터랙션]

디자인:
- Dark theme (BiteEngine 스타일)
- 색상: primary oklch(0.65 0.25 280), accent oklch(0.7 0.2 160)
- 애니메이션: [원하는 효과]
- shadcn/ui 컴포넌트 사용

데이터:
[TypeScript interface]

기술 스택:
- React 19
- Tailwind CSS v4
- Framer Motion
- 한국어 UI
```

---

## 🔧 v0 코드 통합 체크리스트

### 새 컴포넌트 추가 시

- [ ] v0에서 코드 생성
- [ ] 필요한 의존성 확인 및 설치
- [ ] TypeScript 타입 정의 추가/수정
- [ ] 기존 디자인 시스템과 일치하는지 확인
  - [ ] 색상 팔레트 (primary, accent, background)
  - [ ] 폰트 크기 및 weight
  - [ ] 간격 및 여백 (spacing)
  - [ ] border radius
- [ ] 실제 데이터 연결
  - [ ] Mock 데이터 → API/Supabase
  - [ ] Props 인터페이스 정의
- [ ] 애니메이션 확인
  - [ ] Framer Motion 버전 호환성
  - [ ] 성능 (rerender 최소화)
- [ ] 반응형 확인
  - [ ] Mobile (< 768px)
  - [ ] Tablet (768px - 1024px)
  - [ ] Desktop (> 1024px)
- [ ] 접근성 확인
  - [ ] 키보드 네비게이션
  - [ ] ARIA 라벨
  - [ ] 색상 대비
- [ ] 테스트
  - [ ] localhost에서 동작 확인
  - [ ] 다양한 브라우저 테스트
- [ ] 문서화
  - [ ] 컴포넌트 사용 예시 주석 추가
  - [ ] Props 설명

---

## 📦 v0에서 자주 사용할 프롬프트 모음

### 1. 새 페이지 생성
```
BiteEngine의 [페이지명] 페이지를 만들어줘.

레이아웃:
- Header (기존 컴포넌트 재사용)
- [메인 컨텐츠 설명]
- Footer (선택사항)

기능:
- [기능 1]
- [기능 2]

디자인: BiteEngine 다크 테마
```

### 2. 기존 컴포넌트 개선
```
현재 RestaurantCard 컴포넌트를 개선해줘.

현재 코드:
[기존 코드 붙여넣기]

개선 사항:
- 호버 시 확대 애니메이션 추가
- 북마크 버튼 추가
- 이미지 lazy loading
- 성능 최적화
```

### 3. 모바일 최적화
```
현재 투표 페이지를 모바일에 최적화해줘.

문제:
- 카드가 너무 작음
- 사이드바가 화면을 가림
- 버튼이 눌리기 어려움

해결:
- 모바일에서는 single column
- 사이드바를 하단 sheet로 변경
- 터치 영역 확대 (최소 44px)
```

### 4. 다크모드 컴포넌트
```
[컴포넌트명] 다크모드 버전을 만들어줘.

배경: oklch(0.13 0.02 260)
텍스트: oklch(0.95 0.01 260)
Primary: oklch(0.65 0.25 280)
Accent: oklch(0.7 0.2 160)

요구사항: 충분한 대비, 가독성 우선
```

### 5. 로딩/에러 상태
```
레스토랑 목록의 로딩 및 에러 상태 UI를 만들어줘.

로딩:
- 스켈레톤 카드 (shimmer 효과)
- 6개 표시

에러:
- 에러 아이콘
- 재시도 버튼
- 친근한 메시지

빈 상태:
- 일러스트 또는 아이콘
- "아직 등록된 맛집이 없어요" 메시지
- "맛집 추가하기" CTA
```

---

## 🚀 고급 v0 활용법

### A/B 테스트용 변형 생성

```
현재 투표 버튼의 3가지 변형을 만들어줘.

버전 A: 미니멀 (텍스트만)
버전 B: 아이콘 + 텍스트
버전 C: 그라디언트 배경 + 애니메이션

각 버전을 컴포넌트로 분리해서 props로 variant를 받도록 해줘.
```

### 반응형 레이아웃 생성

```
투표 대시보드의 반응형 레이아웃을 만들어줘.

Desktop (> 1024px):
- 3 column grid
- Sidebar 고정

Tablet (768px - 1024px):
- 2 column grid
- Sidebar collapsible

Mobile (< 768px):
- 1 column
- Bottom sheet navigation
```

### 애니메이션 라이브러리 활용

```
투표 완료 시 축하 애니메이션을 만들어줘.

단계:
1. 버튼 클릭 → 스케일 펄스
2. Confetti 효과 (canvas-confetti)
3. 우승자 카드 slide up
4. 예약 정보 fade in

Duration: 총 2초
Easing: spring (framer-motion)
```

---

## 🎨 v0 + Figma 연동 (고급)

v0는 Figma 디자인을 import할 수도 있습니다:

1. Figma에서 디자인 작성
2. Figma → v0 plugin 사용
3. v0에서 코드 생성
4. 프로젝트에 통합

---

## 📝 v0 사용 팁

### 효율적인 작업 방법

1. **프로토타입 먼저**
   - v0에서 빠르게 여러 버전 생성
   - 팀원들과 공유 (v0 링크)
   - 피드백 받고 최종 버전 선택
   - 코드 가져와서 실제 데이터 연결

2. **디자인 시스템 유지**
   - v0에서 생성할 때 항상 BiteEngine 디자인 가이드 명시
   - globals.css의 CSS 변수 사용
   - 커스텀 색상 대신 semantic tokens 사용

3. **코드 리뷰**
   - v0 코드를 그대로 쓰지 말고 검토
   - 불필요한 의존성 제거
   - 성능 최적화 (memo, useMemo, useCallback)
   - 타입 안정성 확인

4. **버전 관리**
   - v0에서 생성한 컴포넌트는 별도 브랜치에서 작업
   - PR로 리뷰 후 merge
   - v0 링크를 PR description에 포함

---

## 🔗 유용한 링크

- v0 공식 사이트: https://v0.dev
- v0 문서: https://v0.dev/docs
- v0 디스코드: https://discord.gg/vercel
- shadcn/ui: https://ui.shadcn.com
- Tailwind CSS v4: https://tailwindcss.com

---

## 💡 실전 예제

### 예제 1: 투표 통계 대시보드 추가

**v0 프롬프트**:
```
BiteEngine용 투표 통계 대시보드를 만들어줘.

표시 내용:
1. 이번 달 총 투표 수 (큰 숫자)
2. 가장 인기 있는 음식 카테고리 (파이 차트)
3. 최근 7일 투표 추이 (라인 차트)
4. Top 5 레스토랑 (순위표)

라이브러리:
- Recharts (차트)
- Framer Motion (애니메이션)
- shadcn/ui Card

디자인:
- 다크 테마
- 2x2 그리드 레이아웃
- Neon indigo/emerald accents
- 반응형 (모바일에서는 세로 스택)

한국어 UI
```

**통합 코드**:
```typescript
// app/analytics/page.tsx
import { VotingDashboard } from "@/components/voting-dashboard"

export default function AnalyticsPage() {
  return (
    <div className="container mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">투표 통계</h1>
      <VotingDashboard />
    </div>
  )
}
```

### 예제 2: 레스토랑 추천 AI 챗봇

**v0 프롬프트**:
```
BiteEngine용 AI 챗봇 인터페이스를 만들어줘.

기능:
- 메시지 입력창 (하단 고정)
- 대화 히스토리 (스크롤)
- 봇 메시지 타이핑 애니메이션
- 레스토랑 카드 추천 (inline)
- "이 맛집에 투표" 버튼

UI:
- 사용자 메시지: 오른쪽 정렬, primary 배경
- AI 메시지: 왼쪽 정렬, card 배경
- 아바타 (AI는 로봇 아이콘, 사용자는 프로필)
- 다크 테마

애니메이션:
- 메시지 fade in
- 타이핑 인디케이터 (3개 점 bounce)
```

---

이 가이드를 따라하면 v0를 활용해서 BiteEngine의 디자인을 효율적으로 관리하고 확장할 수 있습니다!
